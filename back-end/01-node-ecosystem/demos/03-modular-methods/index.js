'use strict'

const colors = require('./lib/colors')
// colors => {makeBlue: function() {}, ...}

colors.makeBlue(/* pass in some args */) // => turn something blue
colors.makeRed(/* pass in some args */) // => turn something red
